import React from 'react';
import Navigation from './Navigation';

const Saved = () =>{
    return (
        <div>
            <Navigation />
            <label>Saved</label>
        </div>);
}

export default Saved